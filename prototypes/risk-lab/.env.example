# Copy to .env only on the server that runs the manager. Never commit .env.
DEEPSEEK_API_KEY=
DEEPSEEK_BASE_URL=https://api.deepseek.com
DEEPSEEK_MODEL=deepseek-v4-flash
# Structured answers and daily plans default to lower-latency non-thinking mode.
DEEPSEEK_THINKING=disabled
