# M0 原型验证记录

日期：2026-09-04；本地复测更新：2026-09-09。结论：**本地原型可运行，V1–V3 仅部分验证通过；M0 尚未整体完成，不能用于真实知识库。** 已补上受控管理员、单账号边界、每日整理的模型执行协议验证，以及 Compose 持久化运行预演；真实模型完成最小问答调用，实际定时整理与 Linux 服务器部署仍未执行。

## 1. 环境与复现

- Windows、Node.js 24.12.0、pnpm 11.19.0；React 19、Vite 7、Vitest 3，精确版本见 `pnpm-lock.yaml`。
- 开发与本地预演服务只监听 `127.0.0.1:4173`，使用独立 `.prototype-data` 数据目录，无真实个人笔记；健康接口只以布尔值报告服务端是否配置模型，不回显密钥。Compose 预演也只发布到回环地址，由宿主机反向代理接入的配置尚未在实际服务器验证。
- `pnpm test`：47 个测试文件、454 项测试通过；完整分项见下表。
- `pnpm build`：TypeScript 检查和 Vite 生产构建通过。
- `pnpm audit --prod`：此次查询未发现已知漏洞，不代表无漏洞或完整安全审计。
- 当前入口 JS 约 683 KB（gzip 215 KB），管理员面板约 4 KB，Markdown 编辑器约 620 KB、导图编辑器约 189 KB，阅读预览继续独立分包；此前单入口约 1.58 MB。完整离线清单 69 项，包含公式字体。初始依赖图缩小不等于真实加载时延/移动端性能验收，完整离线缓存仍下载全部分包与字体。依赖注释仍有两项无效 PURE 注解警告。
- 首轮 GitHub Actions：[运行 33729928112](https://github.com/zfyu222/ZFYSecondBrain/actions/runs/33729928112)，提交 `35b5369`，Ubuntu/Windows 标准运行器均完成锁定安装、当时的 181 项测试、构建和离线清单检查。后续新增测试不自动继承该结论；Linux CI 也不等于用户实际服务器或真实多设备验收。
- 升级保护回归：[运行 33730872052](https://github.com/zfyu222/ZFYSecondBrain/actions/runs/33730872052)，提交 `0ae7cd6`，Ubuntu/Windows 均通过全部 190 项测试、类型检查、生产构建和不可变入口/编辑器分包检查。本记录后的仅文档提交不改变已测代码。
- 富文本阅读回归：[运行 33732472332](https://github.com/zfyu222/ZFYSecondBrain/actions/runs/33732472332)，提交 `404786a`，Ubuntu/Windows 均通过全部 222 项测试、构建和 67 项离线资源检查（包含 CSS 引用的公式字体）。新增场景是静态渲染与自动化检查，不代替浏览器折叠/公式布局实测。
- 正文嵌入回归：[运行 33734382695](https://github.com/zfyu222/ZFYSecondBrain/actions/runs/33734382695)，提交 `8d4bcc4`，Ubuntu/Windows 均通过全部 246 项测试、构建和 67 项离线清单检查。后续附件实现的本地结果不能自动视为该次 CI 结果。
- 附件回归：[运行 33736751505](https://github.com/zfyu222/ZFYSecondBrain/actions/runs/33736751505)，提交 `4c3cb59`，Ubuntu/Windows 均通过全部 289 项测试、构建和 67 项离线清单检查；不代替真实浏览器媒体播放与配额验收。
- OPML 元信息回归：[运行 33737585520](https://github.com/zfyu222/ZFYSecondBrain/actions/runs/33737585520)，提交 `3ce9d87`，Ubuntu/Windows 均通过 306 项测试、构建与离线清单检查。尚未验证其他导图软件的实际导出/导入行为。
- YAML 保留回归：[运行 33738106355](https://github.com/zfyu222/ZFYSecondBrain/actions/runs/33738106355)，提交 `572ce23`，Ubuntu/Windows 均通过 321 项测试、构建与离线清单检查。
- 同目录协调回归：[运行 33738816766](https://github.com/zfyu222/ZFYSecondBrain/actions/runs/33738816766)，提交 `cd8d2ce`，Ubuntu/Windows 均通过 329 项测试、构建与离线清单检查；仍不等同于多进程文件锁。
- 启动保护回归：[运行 33739102532](https://github.com/zfyu222/ZFYSecondBrain/actions/runs/33739102532)，提交 `205afb8`，Ubuntu/Windows 均通过 332 项测试、构建与离线清单检查。
- 本机恢复、可迁移收藏和最近文档回归：[运行 33766171065](https://github.com/zfyu222/ZFYSecondBrain/actions/runs/33766171065)，提交 `0c47bf6`，Ubuntu/Windows 均通过 350 项测试、构建与离线清单检查。

## 2. 自动化结果

| 测试文件                           | 数量 | 已执行内容                                                                                                                                                                                                                                                                        |
| ---------------------------------- | ---: | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `tests/core.test.ts`               |   20 | 中文/特殊字符/多行正文和扩展属性往返；100/500 子节点顺序；同名节点重排改名后关系更新；未知 XML/YAML 拒绝；危险路径与重名；三方合并/删除冲突；普通、Wiki、定义式链接重写和文本附件路径                                                                                             |
| `tests/store.test.ts`              |   10 | 真实 UTF-8 落盘；幂等重试与旧版本拒绝；并发串行；文件应用步骤 1/2/3 异常后的重启回滚；失败后读取不暴露部分文件；恢复遇外部修改停止；移动及重试；非法数据与非空目录拒绝                                                                                                            |
| `tests/text-bytes.test.ts`         |   10 | UTF-8 BOM/CRLF/中文/emoji 真实字节往返；外部文件读取、编辑、移动；三档恢复；BOM 编辑中断回滚；Markdown、OPML、关系 YAML 文件头及正文标记保留；预览元信息及标题；无效 UTF-8 拒绝且保留现场                                                                                         |
| `tests/note-metadata.test.ts`      |    7 | 收藏、标题元数据新增/切换；软链接入口以可读目录列表写入；BOM、CRLF、相邻字段及行内注释保留；未知 Front Matter 字段逐字保留；显式 false；非布尔与未闭合 Front Matter 拒绝                                                                                                                                        |
| `tests/note-tags.test.ts`          |    4 | OFM 标量/列表标签读取；正文井号不推断为标签；非文本、重复和危险标签拒绝；标签新增、删除、BOM/CRLF/行内注释保留及块状标签拒绝                                                                                                                                                           |
| `tests/search.test.ts`             |    2 | 大小写无关的路径/正文关键词；多个 OFM 标签与文本条件组合；Archive 默认排除与显式包含                                                                                                                                                                                               |
| `tests/cil.test.ts`                |    6 | 固定 JSON 命令、只读范围搜索/读取、shell 式命令拒绝、明确授权和路径范围绑定、版本化变更提议只返回建议而不写入；Archive 显式和父范围穿透均拒绝                                                                                                                                          |
| `tests/cil-client.test.ts`         |    4 | CLI 客户端只调用固定本机端点；空白、坏 JSON 与 UTF-8 字节超限在联网前拒绝；服务端 JSON 错误保持失败语义；非 JSON 网关响应不伪装成成功                                                                                                                                                 |
| `tests/summaries.test.ts`          |    4 | raw 到可读 derived 摘要路径映射、YAML 来源版本/来源状态往返、顶层/逐层/原文 1/10 上限及不支持 Archive、每层来源状态强制校验                                                                                                                                                             |
| `tests/manager-review.test.ts`     |   10 | 管理员提议持久审阅、接受后走同一版本事务、初次旧版本拒绝、审阅后来源变化标记过期且不覆盖、拒绝不改原文；HTTP CIL 只读结果绑定来源版本，CIL 变更只生成待审记录；回答引用必须来自实际 read 且来源未变，未读路径拒绝；未配置模型的执行接口拒绝且计划来源不变                                                                 |
| `tests/deepseek-manager.test.ts`   |    6 | OpenAI 兼容模型仅接收 CIL 返回的非归档证据；回复逐字引用在本机验证；连续 Inbox 移动和摘要步骤均经版本化事务；不合格多层摘要退回已校验单层；密钥只来自服务端环境变量；越权分类输出在写入前拒绝                                                                            |
| `tests/memory-workflow.test.ts`    |   12 | 三项首版能力逐项规划、Archive 排除、双视图共同版本检查；服务器本地时间到点运行且每日防重；配置与运行重启持久化、手动重跑；版本绑定摘要写入 derived；高置信度 Inbox 标签、移动与引用重写的单事务；低置信度待确认及来源变化拒绝；双视图结果的版本绑定写入；结果通知持久化、已读、调度失败和旧状态兼容                                                                                          |
| `tests/templates.test.ts`          |    1 | 模板固定保存于 Areas 的可读 raw 子目录；普通 Areas 文档不被误判为模板                                                                                                                                                                                                            |
| `tests/folders.test.ts`            |    3 | 任意层级目录树保留和稳定排序；软链接作为不复制原文的虚拟目录入口参与空间筛选，且即使没有真实文件也会显示在目录树中                                                                                                                                                                  |
| `tests/document-title.test.ts`     |    3 | Markdown 显式标题优先；仅含 OPML 的导图笔记使用其 OPML 标题；缺失标题时保留可读文件名                                                                                                                                                                                              |
| `tests/map-from-markdown.test.ts`  |    2 | 手动生成缺失导图时按 Markdown 标题层级创建节点、保留各段正文；代码围栏中的标题样例不参与生成，且不从层级臆造语义关系                                                                                                                                                                    |
| `tests/markdown-from-map.test.ts`  |    2 | 手动生成缺失 Markdown 时保存 OPML 标题、节点正文与层级；超过 Markdown 六级标题的深层节点保持可读，关系 YAML 不被伪造成正文语义                                                                                                                                                             |
| `tests/local.test.ts`              |   24 | fake-indexeddb 保存重开/标签页版本检查；Markdown 30 分钟粗粒度历史（标注编辑、外部变更或恢复来源）、外部修改前恢复点、恢复前副本及移动后路径迁移；文档组+附件移入/恢复回收站及目标占用拒绝；可读路径+时间戳的最近文档排序及单篇/目录移动迁移；响应丢失重试；提交期间编辑；应急导出严格校验、附件恢复、覆盖前恢复副本及不重放旧传输请求；恢复副本与冲突计划重开；旧冲突升级及未知版本拒绝；选择结果校验、事务写入失败回滚；服务端 409；移动重试 |
| `tests/conflicts.test.ts`          |   13 | Markdown 独立片段选择、双方非冲突内容保留、缺少选择、CRLF/空片段/emoji；OPML+关系成组、兼容组件自动合并、重命名与关系变化、删除对编辑、坏分支拒绝、多图独立选择                                                                                                                   |
| `tests/moves.test.ts`              |   24 | 单篇或任意层级目录移动、远端目录移动期间保留离线编辑、拒绝目标位于源目录内部、连续移动、离线编辑/删除/新增附件与引用、旧名称复用、无基线同名拒绝、目标碰撞、记录丢失、OPML/YAML 重命名、Windows 拼写边界；真实 FileStore + 模拟客户端 API 的移动竞态/丢回执重试；5 个事务边界异常恢复                    |
| `tests/references.test.ts`         |   25 | 精确目标位置、标题/标签重复、转义括号、实体、查询/锚点/文件名井号、代码与 Wiki 转义、GFM 表格；Front Matter 值/注释/CRLF 与原文保留；HTML/未知结构化引用拒绝；公式内伪链接保留、Callout/高亮内实际引用及移动所属文件                                                              |
| `tests/restore.test.ts`            |   23 | 三档目录范围、raw-only 独立恢复、源保持不变、5 个事务边界在新副本恢复；已有/重叠/目录外/链接目标拒绝；中断标记与重开拒绝；损坏日志/散列/账本、未知版本、孤立临时文件及二进制拒绝；实际缺失目录报告                                                                                |
| `tests/preview.test.tsx`           |   27 | 显式路径与标题解析、反向链接按 Wiki/普通链接的实际路径归集、禁止协议/越界、失效提示；React 静态渲染验证 Wiki/普通链接、代码/转义、属性/错误、标题/脚注 ID、媒体占位、HTML/脚本过滤与外部链接保护                                                                                     |
| `tests/rich-preview.test.tsx`      |   29 | Callout 常用/自定义类型、折叠默认状态、嵌套/列表、转义与 HTML 防护；高亮中的强调/链接、标题一致性；行内/块/围栏公式、MathML、错误保留、禁用 URL/图片/HTML 命令、宏隔离/递归与单式/数量/总量上限                                                                                   |
| `tests/embeds.test.tsx`            |   23 | 整篇/标题/嵌套标题、来源相对链接、重复标题/脚注/引用定义隔离、来源属性不污染；循环/深度/数量/总量限制、句中降级、转义/代码/公式、非法目标、移动路径、安全策略与全局公式预算；输入原文保持不变                                                                                     |
| `tests/attachments.test.ts`        |   25 | 0–255 全字节与空文件往返、Base64 规范/限额/路径冲突、整附件二选一；原始字节落盘、v2 标记/旧客户端拒绝、7 处中断恢复、外部修改停止、移动与占用保护、离线对齐、三档及未完成事务新副本恢复                                                                                           |
| `tests/attachment-client.test.tsx` |   13 | 附件+引用同事务保存/重开/同步；重名/路径/旧标签页与模拟写失败；丢回执期间新修改、附件冲突持久化/备份、离线附件随服务端移动、未知协议拒绝；Markdown/Wiki/嵌入来源相对路径、外部媒体禁止、Blob 字节保留及重复媒体数量/字节上限                                                      |
| `tests/attachment-media.test.ts`   |    2 | 受支持图片、音视频与 PDF 的本机 Blob 预览分类；普通 Markdown 链接和不受支持 SVG 均只提供下载，不进入活动预览                                                                                                                                    |
| `tests/api.test.ts`                |    7 | Fastify 注入请求验证附件读取能力、旧/未来写入拒绝、移动占用 400、Host/Origin 403、正文超限 413 与坏 JSON 400；健康接口只报告模型是否配置而不泄漏凭据；不启动监听器或操作当前测试库                                                                                                                                        |
| `tests/opml-metadata.test.ts`      |   18 | 标准/自定义单值头信息与限定名称属性编辑往返、导图专属文档标题可读写、原型链同名属性保留、复杂/重复头元素及不支持声明拒绝；移动时元信息保留、已知 URL 重写、字面邮箱保持、未知受影响引用停止                                                                                           |
| `tests/relation-metadata.test.ts`  |   15 | 无变更逐字保留、节点正文编辑不写关系；端点改名/新增/删除/重复关系重排的注释对应、引号/CRLF/文档注释、map 改名；无效来源映射拒绝、空文件和清空关系处理                                                                                                                             |
| `tests/store-coordination.test.ts` |    8 | 同目录多实例初始化/单次播种、共享基线并发提交、Windows 大小写别名/路径规范化、公开恢复与在途事务排队、失败后的跨实例恢复、错误后队列可用、目录目标在日志准备前拒绝                                                                                                                |
| `tests/startup.test.ts`            |    3 | 真实本机临时端口绑定先于存储初始化、初始化期间 API 503、端口冲突不调用初始化并关闭资源、初始化失败释放端口；两个服务实例不冒充任意多进程文件系统并发验收                                                                                                                          |
| `tests/runtime-config.test.ts`     |    3 | 默认服务目录/端口不变；隔离测试只接受绝对数据目录与受限端口；相对路径、根目录和异常端口在启动前拒绝                                                                                                                                          |
| `tests/dev-watcher.test.ts`        |    1 | 实例化真实 Vite 开发监听器，确认仍监听工程源文件、排除 `.prototype-data` 且不跟随软链接，隔离循环链接不进入监听范围                                                                                                                                                               |
| `tests/map-editing.test.ts`        |   14 | 同名节点排序、整棵子树缩进/提升、关系和扩展属性保留、无效操作、折叠与选中路径；分组输入/重做/关系文件缺失还原、外部替换边界、100 步上限；快捷键与组合输入命令策略                                                                                                                 |
| `tests/offline-shell.test.ts`      |   11 | 生成完整分包/不可变 HTML 清单、固定版本导航、缓存丢失仅请求同版本入口、网络/HTTP 失败保守提示；安装阶段不接管旧页面或清理旧缓存，API/写请求/外部域不拦截；使用 Node 中的模拟 Worker 环境，不冒充真实浏览器                                                                        |
| `tests/offline-status.test.ts`     |    5 | 首次安装、已等待更新、安装属性时序、失败提示、组件卸载与旧安装对象监听清理；不强制刷新或激活                                                                                                                                                                                      |
| `tests/metadata.test.ts`           |   12 | 保留旧快照键序而非按本机排序；原型链同名请求键的提交/移动重试、特殊键内容校验；链接祖先、日志/账本/移动记录硬链接、状态及原文临时硬链接拒绝，其他文件内容保持不变                                                                                                                 |

其中 100/500 节点是序列化正确性测试，**不是交互帧率/触屏性能通过证明**。fake-indexeddb 不是浏览器存储配额、淘汰策略或真机测试。

新增预览测试为 React 静态渲染和纯函数测试，不等同于点击、滚动、布局或 IME 浏览器验收。开发服务返回 200 并复用预览入口；本阶段没有新增浏览器交互验收结论。

管理员与记忆整理面板已接入本机 API，可查看提议前后全文、接受/拒绝，配置三项持续授权与服务器时间、手动形成计划并查看候选。其领域/API 边界已自动化测试；当前构建的本机浏览器复测确认管理员入口和面板分包能够加载，且无浏览器控制台错误。该检查不等同于真实模型交互验收。

2026-09-07 本地复测还完成以下可复现检查：

- `pnpm typecheck`、`pnpm test`（47 个测试文件、454 项）、`pnpm build` 与构建离线清单检查均通过；依赖中的两条 Rollup `@__PURE__` 注释警告仍为上游构建提示。
- 构建预览在 `127.0.0.1:4173` 启动，健康接口返回协议版本 2；管理员按需分包以二进制流响应，避免 Fastify 将 JavaScript 当作 JSON 序列化。新增自动化回归覆盖该响应路径。
- `pnpm browser:smoke` 使用独立无痕 Chrome 上下文读取 `127.0.0.1`：健康接口、原文编辑区及管理员面板均成功加载；标准 Markdown 导出按钮实际生成非空下载文件。在健康状态确认模型已配置后，脚本只填写问题并确认发送按钮可用，绝不提交。它不调用模型、不写入知识库，也不使用日常浏览器的 IndexedDB；覆盖真实浏览器静态资源、下载入口与按需面板加载，不替代编辑、同步、媒体或移动端交互验收。
- `pnpm browser:auth-smoke` 在临时数据目录与 `127.0.0.1:48175` 启动生产服务：未登录时显示密码界面，错误密码被拒绝，正确密码进入编辑界面；随后同一浏览器会话可读取受保护快照，编辑未同步草稿后 `beforeunload` 与退出登录都会出现保护确认。该检查发现并修复了认证中间件错误拦截会话接口和静态页面的缺陷；它不代替真实 HTTPS、反向代理或密码轮换验收。
- `pnpm browser:sync-smoke` 在另一独立临时服务及两个隔离 Chrome 上下文中完成登录、新建 Inbox 笔记、修改标题、同步，以及第二个浏览器读取同一笔记。它为首版的最小跨浏览器保存/同步闭环提供真实页面证据；不以它代替并发编辑、冲突选择、离线写入或移动设备矩阵。
- `pnpm browser:conflict-smoke` 用两个隔离 Chrome 上下文对同一已同步 Inbox 笔记作不同标题修改：本机一侧先模拟断网，另一侧完成服务器同步，恢复本机联网后页面显示“保留哪一版”和显式“保存选择”入口。它证明真实浏览器流程不会静默覆盖冲突；不替代每一种正文、附件、移动与删除冲突组合。
- `pnpm browser:move-smoke` 在隔离生产服务中创建 Markdown 笔记、添加 GIF 附件、启用导图并记录共同基线，随后移动整组文档。浏览器确认导图中的附件引用会触发原始 GIF 下载，移动完成后同步；宿主测试目录确认目标 Markdown 与同名附件目录均落盘。该流程还修复了导图 `zfyBody` 附件引用和双视图基线时间戳被误判为未知结构化引用而阻止移动的问题；它不替代所有复杂 OFM、外部结构化字段或中断恢复组合。
- `pnpm browser:dual-view-smoke` 在隔离生产服务中修改导图根节点正文，再从 Markdown 新增一个子层级并执行“按 Markdown 增量同步”。页面确认导图独立正文仍保留、新节点已出现；该流程验证共同基线的节点级合并不会把非重叠导图编辑整份重建掉。重名节点、改名、删除或同字段并发编辑仍会安全停止，等待用户手动处理并记录新基线。
- `pnpm browser:tab-smoke` 在同一 Chrome 上下文打开两个标签页，第一标签页修改已存在的 Markdown，第二标签页通过版本广播自动载入新正文；验证同源标签页不会长期停留在旧快照。
- `pnpm browser:media-smoke` 在隔离生产服务中上传 GIF、WAV、MP4 和 PDF，生产页面预览区实际出现图片、音频、视频控件及 PDF Blob 查看器；视频夹具由本机 FFmpeg 生成，所有原始附件仍保留下载入口，并在 390px 窄屏确认无横向溢出。该检查验证桌面 Chrome 的首版媒体路径，不替代移动端真实播放器、异常编码和大文件兼容矩阵。
- `pnpm browser:offline-smoke` 在独立数据目录和端口完成首次联网缓存，等待 Service Worker 接管后停止临时服务，再从根入口成功打开原文编辑区。Worker 只接管根导航，未缓存的新 URL 离线失败是有意保护版本一致性的边界。它提供真实桌面浏览器的离线重开证据，但不替代首次离线安装、缓存淘汰、移动端或写入同步验收。
- 同日以绝对 `ZFY_DATA_DIR` 和 `ZFY_PORT=48173` 实际启动第二个回环服务；该端口的 Host/Origin 白名单随端口收紧，健康接口与同一只读浏览器 smoke 均通过。隔离目录只用于测试，未读取或写入默认 `4173` 服务的数据。
- 使用版本化离线 HTML 打开当前构建，确认本地同步完成后可见“管理员与每日整理”。旧标签页继续停留在其已缓存版本是有意保护未保存编辑的行为；升级应先保存或导出草稿，再关闭旧标签页重新打开，不能用强制刷新或清除浏览器数据替代。
- 本机浏览器切换“模拟断网”后，已缓存 Markdown 仍可编辑；同步、附件新增、模板、回收站、归档、缺失视图生成、双视图基线/转换和管理员入口均禁用。关闭模拟断网后附件入口恢复可用，离线新建的路径策略由回归测试固定为 `raw/Inbox`。
- 展开管理员面板并检索测试资料“原型”：页面显示来源快照版本、`raw/Areas/原型说明.md` 原文片段及可解析的关联 `raw/Areas/开始使用.md`。页面明确标注结果是证据而非伪造的模型回答。
- 2026-09-08 在本机安装 Docker Desktop 与 WSL 2 后，实际构建 Compose 镜像（约 711 MB）并启动隔离数据目录。使用配置的 HTTPS Host 请求健康接口返回 200；未登录快照返回 401、错误 Host 返回 403。容器重启后宿主机挂载目录仍有 `raw`、`derived`、`state` 与版本标记。测试容器已停止且隔离数据保留；这不代替 Linux、反向代理证书、真实域名或远程移动浏览器验收。
- 同日部署至腾讯云 TencentOS Server 4.4 测试机：Docker Compose 应用仅映射到服务器 `127.0.0.1:4173`，Caddy 以 IP 自签名证书反代 `https://9.135.9.109`。从本机验证 443 端口、健康接口 200、未登录快照 401；独立 Chrome 接受测试证书后，错误密码拒绝、正确密码进入编辑界面。容器重启后宿主机持久目录仍有 `raw`、`derived`、`state`，再次远程登录通过。IP 自签名证书会触发浏览器警告，不是受信任 HTTPS、域名证书或真实移动设备验收。
- 同日重建远程 Compose 镜像以应用 AI 运行时配置；新浏览器会话确认管理员面板已识别模型可用。对已命中的测试原文发起真实问答后，页面返回答案、来源版本及 `raw/Areas/原型说明.md` 的逐字引用；未命中的问题在模型调用前被拒绝，知识库未被写入。这证明测试机的端到端只读问答与引用复核，不代表真实资料的提示注入防护、成本上限、长任务或自动整理验收。
- 2026-09-08 通过本机 API 写入隔离的一像素 GIF 附件及 Markdown 引用，再在 `127.0.0.1` 构建页面重载并打开该笔记：预览区域显示图片，且显示对应的下载入口。验证了受支持图片字节从服务端快照到浏览器 Blob URL 的实际页面路径；不以此代替音视频/PDF、异常编码、移动端播放器或大文件验收。
- 同日通过本机 API 写入根节点和 500 个子节点的隔离 OPML，再在浏览器打开导图：关系目标控件显示 501 个候选项，点击折叠后画布仅保留根节点，页面没有控制台错误。此结果确认大夹具的实际加载和折叠路径，不提供交互帧率、触屏或中文 IME 性能结论。
- 同日以 `127.0.0.1` 内置浏览器和 `localhost` 独立 Chrome 客户端连接同一服务。Chrome 侧的独立本机副本与服务端版本不同，页面进入冲突态，明确展示本机与服务端全文以及“保存选择”入口，未自动覆盖。该现场保留未选择，不以它代替双方同时编辑、不同浏览器/设备矩阵或冲突选择后的同步验收。
- 同日新增受支持 PDF 的本机预览路径：仅 Markdown 图片形式的已校验附件生成 Blob URL，页面同时显示浏览器查看器容器、下载原件入口和查看器失败说明。隔离的 1 页 PDF 已用本机 Poppler `pdfinfo` 验证为未加密且无 JavaScript；当前自动化浏览器没有暴露其 PDF 查看器内容，因此这不证明每个浏览器的实际 PDF 渲染兼容性。
- 同日使用本机已具备的 FFmpeg 生成隔离的 1 秒 MP3 与 MP4 样本并写入测试库；`127.0.0.1` 页面显示音频原生播放/进度控件、视频缓冲与控制区，以及两者的下载入口。它验证受支持字节经 Blob URL 到浏览器原生媒体控件的页面路径；不代表已验证可听声音、视频画面、编解码矩阵、移动端播放器或异常媒体恢复。
- 同日配置本机忽略的 `DEEPSEEK_API_KEY` 后，向 DeepSeek OpenAI 兼容端点发起一次最小问答。模型仅收到命中的 `raw/Areas/原型说明.md`，返回回答及逐字引用；服务端再次校验引用路径、片段和来源版本后才返回页面。未命中确定性本机证据的问题在联网前被拒绝。该检查不代表提示注入、长任务、成本控制或自动整理已验收。
- 同日新增模型驱动的每日整理执行器：连续处理时先完成明确 Inbox 移动并把未处理摘要候选推进到新路径，再使用推进后的版本生成摘要；回归测试确认两个模型结果都只能通过既有版本化事务落盘。该结果使用模拟模型响应，不代表真实模型的定时执行、格式稳定性、成本或失败恢复已验收。
- 同日用独立临时知识库调用真实 DeepSeek：多层摘要两次未通过严格 1/10 长度检查，服务未写入；随后模型返回一个经验证的单层语义标题，计划完成并只新增 `derived/summaries` 文件。该结果证明真实模型调用和拒绝/回退路径，不代表真实定时、完整多层摘要质量、成本或失败恢复已验收。
- 2026-09-09 完成双视图增量同步的节点级合并：Markdown 新增层级与导图独立节点正文可并存，反向同步也按 Markdown 前置区/标题块合并，重叠修改安全拒写；Markdown 来源导图的内部合成根、原始 Front Matter（含标签、收藏和自定义字段）在往返时保留。生产构建下 `pnpm browser:dual-view-smoke` 通过；全量回归增至 462 项。该结果不等同于完整 GraphRAG 或复杂节点改名/删除的自动合并验收。

附件阶段还使用真实隔离磁盘、fake-indexeddb 和 Fastify 注入请求；媒体字节样本不冒充有效视频/图片解码验收。文件选择器、对象 URL 生命周期、图片解码和播放器真机交互仍待验证。服务已切换构建预览，根路径返回 200、健康接口报告 `protocolVersion: 2`；没有重新载入或清理用户浏览器数据。

收尾读取开发日志发现监听器曾跟随异常测试的循环 junction 导致 `ELOOP`。已排除 `.prototype-data` 并关闭监听器软链接跟随，未删除夹具。重新启动实际开发服务后再次执行 12 项元数据测试（含循环链接），根路径仍返回 HTTP 200；另有上述独立监听器测试。该修复只约束开发监听，不宣称增加了生产访问控制或文件系统并发锁。

测试首次发现 OPML 数值实体未解码，导致换行/制表符变为字面字符串；已修复解析选项并加入回归测试。还补充了读取前事务恢复、移动幂等请求持久化、冲突选择前备份和编辑禁用保护。

## 3. 实际浏览器检查

使用本机浏览器打开构建后的应用，不以第三方框架演示页替代。

- Markdown 编辑并出现“已保存本机”后刷新：原文仍存在。
- 导图输入两段正文和 emoji，新增子节点、撤销，再刷新打开：正文和结构保存正确。
- 首次无共同基线，服务端与本机同名内容不同：明确提示冲突；选择本机后保存，再次同步成功。
- 移动 `raw/Inbox/开始使用.md` 至 `raw/Areas/开始使用.md`：UI 报告成功；原型说明中的入站 Wiki 链接同步改为新路径，核对服务端原文一致。
- 关闭本轮启动的测试服务后刷新：已缓存界面仍打开，原有本机 Markdown 可读取。随后重新启动服务。
- 桌面截图检查：侧栏、双栏编辑/预览、同步状态和验证工具可见。
- `pnpm browser:smoke` 还以 390×844 窄屏加载页面：同步按钮可见，页面 `scrollWidth` 不超过 `clientWidth`，无整页横向溢出。该检查修复了操作栏未换行造成的真实溢出；不代替安卓真机验收。

本轮新增浏览器验收（独立生成样本，不修改已有示例）：

- Markdown 双方都修改中间段，同时分别修改开头和结尾；选择服务端中间段后，双方独立段都保留，核对服务端实际落盘原文一致。
- 未选冲突项即保存：页面明确拒绝，不清空待处理状态；完成选择后可以保存并同步。
- 本机改名导图节点并更新关系，服务端修改关系类型：只显示一组两个选项，而不是两个文件各自选择。
- 刷新页面后该冲突组仍在；选本机组并同步，服务端 OPML 与 YAML 的新节点路径一致，未混入旧端点。
- 本轮页面检查未捕获 JavaScript error 日志。浏览器使用同一客户端，服务端另一侧修改由隔离夹具脚本构造，不冒充两台真实设备测试。

复现入口：`pnpm --filter @zfy/risk-lab exec tsx tests/browser-conflict-fixture.ts base 唯一英文测试标签`，界面同步并修改对应样本后，用同一标签执行 `remote` 阶段制造另一侧修改。脚本仅接受本机原型服务，保留其他文件；拒绝覆盖已存在或发生额外变化的样本。

未使用系统级断网，服务不可用刷新测试不等于浏览器全网络离线、首次离线安装或移动端缓存淘汰验收。中文字符输入不等于真实中文 IME 组合输入验收。测试写入的示例留在忽略目录/本机测试存储中，未进入 Git。

## 4. 尚未通过的门槛

| 门槛          | 状态     | 继续前必须补齐                                                                                                                                                                                                                                            |
| ------------- | -------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| V1 格式与编辑 | 部分通过 | 已补充 Wiki/标题/属性、Callout/高亮/公式及正文嵌入、安全本机附件入口和导图结构/类型/撤销；标题、标签、收藏和软链接的更新会逐字保留未知 Front Matter 字段。本机生产浏览器已验证 GIF 图片、WAV 音频、MP4 视频控件、PDF Blob 查看器及下载入口，以及 500 节点夹具的加载与折叠；文档嵌入已提供原生键盘可操作的折叠控件和“打开原文”入口，导图关系边可见性已使用路径索引。剩余异常媒体与移动端真实播放器兼容、公式布局、500 节点交互性能、IME 和真机触屏；跨软件 OPML 兼容 |
| V2 离线与同步 | 部分通过 | 已补齐 Markdown 片段选择与非冲突保留、OPML+关系一致组，并在两个独立本机浏览器客户端验证服务器差异进入可见冲突态而非静默覆盖；客户端双视图已具备共同基线、指纹和节点级增量合并，同一浏览器标签页已具备版本广播与自动重载。剩余双方同时编辑的全组合、真实多设备、真实配额失败、标签页浏览器矩阵、跨版本客户端隔离、离线缓存升级，以及服务端/多设备级增量协议 |
| V3 路径与恢复 | 部分通过 | 已有可读移动链/旧路径提交、OPML 正文/URL 和关系 map、部分 Front Matter 与精确 Markdown 目标重写、三档 UTF-8/二进制夹具恢复；剩余图谱/自定义元数据 schema、HTML/完整 OFM 覆盖、大文件流式/增量协议、真实多设备/断电失败、正式历史/回收站与真实整库恢复演练 |
| V4 AI         | 部分执行 | 已有不暴露文件系统的 CIL 固定命令、只读范围、任务授权、持久审阅及应用前二次验版测试；管理员和同步 API 的会话失效会回到登录页并保留本机草稿。剩余真实 Agent/模型隔离、提示注入、事实回答来源链、管理员关闭和长任务验收 |
| V5 原生复用   | 未执行   | Windows/Android 壳及原生控件真机验证                                                                                                                                                                                                                      |
| V6 Linux 服务器 | 部分执行 | 本机与腾讯云 TencentOS 测试机均已验证镜像构建、回环应用端口、登录边界、持久目录、容器重启和 IP 自签名 HTTPS；剩余受信任域名证书、反向代理长期运维、真实移动设备与正式网络条件验收                                                                                              |

特别限制：Markdown 采用行序列形成的冲突片段，不做字符级合并；导图/关系保持成组一致，但尚未做图内节点/字段级合并。组内另一分支的修改可能只保留在恢复副本中；完整双方可导出。缺失共同基线、删除对编辑保持保守选择，未引入 AI 合并或进阶 diff 查看器。

版本测试还发现：仅增加 Dexie 数据库版本不能保证旧客户端无法重新打开，因此没有把它当作写入隔离方案。现在的生产 Worker 不再跳过等待期或接管未受控页面；根导航固定使用同构建的不可变 HTML，不从可变的 `/` 混入新代码。更新提示要求先保存/导出，再关闭全部原型标签页重开；不刷新正在编辑的页面，不删除 IndexedDB。

这只是生产界面升级策略的模拟生命周期与构建产物验证，不是完整客户端版本隔离。首次未受控页面、开发模式、旧版 Worker 向本版迁移、浏览器强制更新/缓存淘汰、后端协议及真实新旧标签页仍待验收。升级前须关闭旧版标签页，不能宣称所有版本可并存写入。

外部进程可以在版本检测之后写入文件，当前没有操作系统级排他锁。因此不得直接让外部编辑器和原型服务并发管理真实原文。异常注入测试仅证明已列出的回滚分支，不证明物理断电一致性。

## 5. 下一步

冲突处理、可读移动链/旧路径同步、精确引用和三档文本副本恢复子任务已有测试。接下来继续编辑/预览与异常恢复验收，并保留上述同步门槛，再推进正式笔记闭环。移动阶段使用隔离磁盘夹具与模拟客户端网络，不宣称真实多设备验收通过。恢复实验不接入旧同步服务，也没有创建备份的功能；目录字节复制不等于尚未实现的历史/配置语义已恢复。原型已有可选的本地单账号边界和 Compose 部署预演；真实 AI、生产级身份体系与实际 Linux 服务器部署仍不在已验证范围。

运行方式见[原型说明](../../prototypes/risk-lab/README.md)，当前 schema 与错误边界见[契约](./prototype-contracts.md)。已确认的产品需求没有因原型限制而删减。
